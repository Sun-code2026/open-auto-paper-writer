import React from 'react';
import ReactDOM from 'react-dom/client';
import { PaperWriter } from './PaperWriter';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PaperWriter />
  </React.StrictMode>,
);
